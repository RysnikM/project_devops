server {
    listen 80 default_server;
    server_name 92.119.115.101;
}
server {
    location / {
        include proxy_params;
        proxy_pass http://unix:/run/gunicorn.sock;
    }

    location = /favicon.ico { access_log off; log_not_found off; }

    location /static/ {
        root /home/mvlab/myprojectdir;
    }
}  